app = "revey-ui"
primary_region = "sin"

[http_service]
  internal_port = 3000
  force_https = true
  min_machines_running = 1

  [[http_service.checks]]
    path = "/"
    method = "GET"
    protocol = "http"
    grace_period = "10s"
